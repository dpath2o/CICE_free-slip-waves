# Coastal drag + free-slip: status update for Mathieu & JF (Fri, 19 Dec 2025)

## 1) Overview 
- Re-sync on where I left the coastal-drag / boundary-condition work in late Oct.
- Confirm the immediate “next coding steps” (so I don’t spin wheels after a long pause).
- Quick reminder: I would like us to push the manuscript to submission for my Antarctic fast ice study; I need your final (?) review.

- Issue #1 (free-slip formulation / masking / test videos):
  https://github.com/dpath2o/CICE_free-slip/issues/1
- Issue #2 (coastal drag experiment + diagnostics checklist):
  https://github.com/dpath2o/CICE_free-slip/issues/2

---

## 2) Where I left this (end of Oct 2025)

### 2.1 Free-slip boundary condition: discrete formulation drafted + idealised sanity tests
I wrote up most of the work for **no-slip** vs **free-slip** handling of velocity values that appear in *finite-difference stencils* near the land boundary.

Key points:
- **Impermeability is always enforced** via masks on the *normal* flow at solid faces:
  - `u_E <- m_E * u_E`
  - `v_N <- m_N * v_N`
- The remaining question was how to treat **masked neighbours in derivative/strain-rate stencils**.
  - **No-slip:** if neighbour lies on land, clamp neighbour to `0`.
  - **Free-slip:** if neighbour lies on land, set neighbour equal to local value (even reflection), so the discrete normal gradient vanishes.

This was written down in a simple 2×2 C-grid schematic (T/E/N/U points) with the 'effective neighbour' recipe expressed using face masks (`m_E`, `m_N`).

I also attached idealised animations comparing *free-slip* vs *no-slip* behaviour for a **uniform eastward wind forcing** case on a small uniform grid (pre-coastal-drag).

---

### 2.2 Coastal drag: initial experiment definition
I created a minimal 'Experiment 1' checklist:
- **Enable coastal drag** and confirm new diagnostic outputs appear as expected:
  - `F2E`, `F2N`
  - `KuxE`, `KuyE`
  - `KuxN`, `KuyN`

This was intended as the first end-to-end confirmation that:
1) the code paths are active, and 
2) the diagnostics are being computed on the correct staggered locations (E and N faces).

---

## 3) What I plan to do next (proposed execution order)

### Step A — Re-orient and reproduce the Oct baseline (idealised)
- Pull the latest state of the sandbox branch/repo used in October.
- Re-run the idealised uniform-wind test(s) to confirm I can reproduce the free-slip vs no-slip differences shown in the animations.

**Deliverable:** short note confirming baseline reproducibility + the run configuration(s) used.  
**Target:** next week.

### Step B — Implement / finalise the free-slip option in the stencils that matter
- Identify the exact strain-rate / stress-divergence stencil locations where land-adjacent neighbours are currently handled as no-slip (clamped).
- Add a clean switchable implementation of the *effective neighbour* rule:
  - no-slip (Dirichlet): neighbour on land → 0  
  - free-slip (Neumann): neighbour on land → local value (even reflection)

**Deliverable:** minimal, reviewable PR/diff that toggles no-slip vs free-slip without involving coastal drag.  
**Target:** next week.

### Step C — Turn on coastal drag and validate diagnostics (Experiment 1)
- Enable coastal drag.
- Verify diagnostics are present, non-zero where expected, and structurally sensible:
  - `F2E/F2N`: consistent with coastal geometry / form-factor assumptions on faces
  - `Kux*/Kuy*`: responds to flow orientation and proximity to coast

**Deliverable:** a small set of diagnostic plots + a brief interpretation (what looks right / what looks wrong).  
**Target:** next week.

### Step D — Controlled matrix: boundary condition × coastal drag
Run a small matrix to isolate interactions:
- no-slip + no coastal drag (baseline)
- free-slip + no coastal drag
- no-slip + coastal drag
- free-slip + coastal drag

**Deliverable:** short comparison focusing on near-coast speed, shear, and any stability/performance impacts.  
**Target:** next week (or early January, depending on runtime/queue).

### Step E — Real-world standalone test case (ACCESS-OM3 geometry)
Use ACCESS-OM3 grids/bathymetry/landmask to run this standalone CICE branch and assess:
- `F2E/F2N` patterns on an Antarctic-relevant grid
- implications for Antarctic fast-ice diagnostics (speed field behaviour near complex coastlines)

Links:
- ACCESS-OM3 configs: https://github.com/ACCESS-NRI/access-om3-configs  
- CICE branch: https://github.com/dpath2o/CICE_free-slip

**Deliverable:** concise validation note + key figures/maps.  
**Target:** January.

### Step F — Real-world coupled test case (MOM6–CICE6, ACCESS-OM3)
- Integrate the preferred BC/coastal-drag configuration into the coupled setup.
- Run a short coupled experiment to check for regressions and qualitative behaviour near the Antarctic coast.

**Deliverable:** draft “methods + initial results” text/figures suitable for a paper update.  
**Target:** February–March.

---

## 4) Antarctic fast ice sensitivity study 
I would like to **submit the paper** as soon as we can converge on final edits.

+ When you get the chance can you please review the current manuscript draft (especially the Methods and Appendix which I've now corrected).

### Appendix: “Motivation and Regridding Strategy” (fast-ice diagnostic)
Because the **B-grid no-slip** in the version of CICE (6.4.1) standalone creates corner-based “dead zones” (speed artefacts) adjacent to land, I clarified the speed diagnostic used for fast ice classification:

- Compute native B-grid speed magnitude: `|u_B| = sqrt(u_ice^2 + v_ice^2)`.
- Build T-grid speed options from (`u`,`v`):
  - **Ta:** simple 2×2 box-average of B-grid corners around each T-cell.
  - **Tb:** (JF-method) a no-slip–aware 2×2 average that treats land-corner values as zero *before* averaging (denominator still 4).
  - **Tx:** bilinear B→T regridding via precomputed ESMF weights, then magnitude.

For the experiments in the paper, I’m using **Tb exclusively** as the fast ice speed diagnostic:
- `A_ice > A_thresh` and `0 < |u_Tb| <= u_thresh`

Rationale:
- **Tb** suppresses the unphysical “dead” T-cells right next to land that arise from corner masking,
- while still respecting the intended no-slip behaviour and co-locating speed with T-grid concentration.
